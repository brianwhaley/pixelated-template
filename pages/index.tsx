export default function Home() {
  return (
    <main style={{display:'flex',flexDirection:'column',justifyContent:'center',alignItems:'center',minHeight:'70vh'}}>
      <h1>Pixelated Template</h1>
      <p>Welcome — this template is served from <code>brianwhaley/pixelated-template</code>.</p>
      <p>Run <code>npm run setup &amp;&amp; npm install &amp;&amp; npm run dev</code> to get started.</p>
    </main>
  )
}
